client:
  cache: null
  domain: []
  impl: api
  postman:
    meta_data:
      headers: null
      impersonate: chrome
      proxies: null 
    type: curl_cffi
  retry_times: 5
dir_rule:
  base_dir: ${JM_DOWNLOAD_DIR}
  normalize_zh: null
  rule: Bd_Pname
download:
  cache: true
  image:
    decode: true
    suffix: .jpg
  threading:
    image: 30
    photo: 20
log: true
plugins:
  valid: {}
version: '2.1'
